import LoadingSpinner from './loading.gif';
import SideClick from './slide-click.wav';
import InterfaceClick from './interface-click.wav';

const Images = {
  LoadingSpinner,
};

export const SoundTracks = {
  SideClick,
  InterfaceClick,
};

export default Images;
